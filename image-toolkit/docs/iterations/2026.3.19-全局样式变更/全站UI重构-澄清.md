# 全站 UI 重构 - 需求澄清

## 基本信息

| 项目         | 值                                 |
| ------------ | ---------------------------------- |
| **功能名称** | 全站 UI 现代化重构 (Glassmorphism) |
| **所属迭代** | 2026.3.19-全局样式变更             |
| **产品经理** | @PM                                |
| **澄清日期** | 2026-03-19                         |
| **状态**     | ⏳ 待回答                          |

## 原始需求

> 客户要求将之前的 `prototype_all_views.html` （Glassmorphism 玻璃拟态设计）风格推广到整个 Electron 应用中，需具备现代高级感，并补充人性化操作体验（如深浅模式持久化、长文本处理、防呆提示等）。

---

## 澄清问答

请在每个 **A (Answer)** 后填写你的期望或决定，也可以留空由 AI 按照行业最佳实践（默认处理）来实现。

### Q1: 系统头部交互 (Titlebar & Draggable Area)

当前原型是纯网页展示，未体现桌面端控制。我们是否需要**彻底隐藏 Electron 默认的标准标题栏**（即变为无边框窗口 `frame: false`），并自己实现左侧侧栏顶部的“可拖拽区 (drag region)”以获得最大沉浸感？
**A**: [是，采用无边框设计]

### Q2: 组件库弹窗与下拉框的深度覆盖

Naive UI 的大量内置组件（如 Select 下拉菜单体、Modal 弹窗背景）都有复杂的遮罩层次。这部分悬浮层我们需要**深度覆写加上 `backdrop-filter` 变成毛玻璃质感**吗？还是为了保证渲染性能，悬浮表单层保持原有的不透明实色？
**A**: []

### Q3: 窗口圆角与阴影叠加 (App Bounds)

如果要做到类似 macOS 的极致沉浸感，我们需要让 Electron 渲染进程的 `body` 背景完全透明（`transparent: true`），由最外层 div 绘制圆角和应用阴影。是否采用这种高级特性？（注：这可能会在某些旧版 Windows 上出现黑边）
**A**: [请在这里回答，或留空默认：不开启窗口透明，在应用内部渲染圆角即可]

### Q4: 主题配置的存储隔离与启动防闪烁

用户希望“深浅模式”持久化跟随。我们需要把主题设置与其他业务配置隔离开吗，以便在应用启动加载完 Vue 庞大组件树之前，就能瞬间读取并渲染底色，**实现 0 闪烁启动**？
**A**: [请在这里回答，或留空默认：需要，利用 preload 脚本或轻量化 storage 拦截白屏]

### Q5: 关于图片格式转换 (`FormatConvert.vue`) 的旧有特性去留

文档提到“格式转换”将复用“文档转换”并排的下拉框。但**图片转换**通常还包含“尺寸调整”、“画质百分比”等特有设置。这些图片独有设置项是作为“浮动的高级选项面板”展示，还是直接在中间区域另起一个平铺卡片展示？
**A**: [请在这里回答]

---

## 待确认项

- [ ] 填写上述五个维度的问题。

## 关联文档

- [需求规格说明书](../需求规格说明书.md)
- [交互设计参考原型](../../2026.3-功能迭代-文件格式转换/prototype_all_views.html)
