# Electron 图片工具库 - 功能测试用例

## 测试概述

| 项目     | 说明                                       |
| -------- | ------------------------------------------ |
| 功能模块 | ImageKit（SVG 查看 / 图片压缩 / 格式转换） |
| 需求版本 | V1.0                                       |
| 用例数量 | 24                                         |
| 编写日期 | 2026-03-16                                 |

## 测试范围

### 覆盖需求

- [x] REQ-001: SVG 文件夹批量加载
- [x] REQ-002: SVG 宫格/列表视图切换
- [x] REQ-003: SVG 搜索过滤
- [x] REQ-004: SVG 批量改色
- [x] REQ-005: 图片多格式压缩
- [x] REQ-006: 三种压缩模式
- [x] REQ-007: 格式互转
- [x] REQ-008: 预设尺寸
- [x] REQ-009: 拖拽文件加载
- [x] REQ-010: 快捷键操作

### 不测试范围

- Worker 线程池性能（优化项，未实现）
- ICO 多尺寸嵌入（延后功能）
- macOS DMG / Linux AppImage 打包
- 自动更新功能

---

## 冒烟测试（P0）

### TC-001: 应用启动

| 属性     | 值   |
| -------- | ---- |
| 优先级   | P0   |
| 类型     | 冒烟 |
| 关联需求 | 全局 |

**前置条件**：

1. Node.js ≥ 20 已安装
2. `npm install` 已执行

**测试步骤**：
| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 执行 `npm run dev` | 终端输出 `dist-electron/main.js built`，无报错 |
| 2 | 等待 Electron 窗口弹出 | 窗口显示 ImageKit 侧栏 + 空状态页 |
| 3 | 点击侧栏三个菜单项 | 页面平滑切换，无白屏 |

---

### TC-002: SVG 文件夹加载

| 属性     | 值      |
| -------- | ------- |
| 优先级   | P0      |
| 类型     | 冒烟    |
| 关联需求 | REQ-001 |

**前置条件**：

1. 准备含 10+ 个 SVG 文件的文件夹

**测试步骤**：
| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 点击「选择文件夹」 | 弹出文件夹选择对话框 |
| 2 | 选择 SVG 文件夹 → 确认 | 宫格视图显示所有 SVG 图标 + 文件名 |
| 3 | 验证文件数量 | 消息提示「已加载 N 个 SVG 文件」 |

---

### TC-003: 图片压缩基本流程

| 属性     | 值      |
| -------- | ------- |
| 优先级   | P0      |
| 类型     | 冒烟    |
| 关联需求 | REQ-005 |

**前置条件**：

1. 准备 1 张 PNG 文件（> 100KB）

**测试步骤**：
| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 切换到「图片压缩」页 | 显示空状态引导区 |
| 2 | 点击「选择图片」→ 选择 PNG | 文件列表显示文件名、大小、「待处理」标签 |
| 3 | 点击「开始压缩」 | 按钮变为 loading，文件标签变为「压缩中…」 |
| 4 | 等待压缩完成 | 显示 -XX% 绿色百分比 + 压缩后大小 |
| 5 | 查看汇总面板 | 显示原始总大小、压缩后总大小、节省百分比 |
| 6 | 验证按钮恢复 | 「开始压缩」按钮恢复为可点击状态 |

---

### TC-004: 格式转换基本流程

| 属性     | 值      |
| -------- | ------- |
| 优先级   | P0      |
| 类型     | 冒烟    |
| 关联需求 | REQ-007 |

**前置条件**：

1. 准备 1 张 PNG 文件

**测试步骤**：
| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 切换到「格式转换」页 | 显示空状态引导区 |
| 2 | 点击「选择图片」→ 选择 PNG | 文件列表显示，格式卡片出现 |
| 3 | 点击 WebP 格式卡片 | WebP 卡片高亮选中 |
| 4 | 点击「开始转换」 | 文件标签变为「→ .webp」 |
| 5 | 验证输出文件 | 同目录下生成 .webp 文件 |

---

## 功能测试（P1）

### TC-005: SVG 文件选择模式

| 属性     | 值      |
| -------- | ------- |
| 优先级   | P1      |
| 类型     | 功能    |
| 关联需求 | REQ-001 |

**测试步骤**：
| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 点击「选择文件」 | 弹出文件选择器，只显示 .svg 文件 |
| 2 | 多选 3 个 SVG 文件 → 确认 | 宫格显示 3 个 SVG |
| 3 | 再次点击「选择文件」→ 选 2 个 | 追加到现有列表，共 5 个 |

---

### TC-006: SVG 视图切换

| 属性     | 值      |
| -------- | ------- |
| 优先级   | P1      |
| 类型     | 功能    |
| 关联需求 | REQ-002 |

**测试步骤**：
| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 加载 SVG 文件夹 | 默认宫格视图 |
| 2 | 点击列表图标 | 切换为列表视图，显示文件名 + 大小 |
| 3 | 点击宫格图标 | 切回宫格视图 |

---

### TC-007: SVG 搜索过滤

| 属性     | 值      |
| -------- | ------- |
| 优先级   | P1      |
| 类型     | 功能    |
| 关联需求 | REQ-003 |

**测试步骤**：
| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 加载含 20+ SVG 的文件夹 | 全部显示 |
| 2 | 搜索框输入 "icon" | 实时过滤，只显示文件名含 "icon" 的 |
| 3 | 清空搜索框 | 恢复所有文件 |
| 4 | 输入不存在的关键字 | 显示空状态「没有匹配的 SVG 文件」 |

---

### TC-008: SVG 批量改色

| 属性     | 值      |
| -------- | ------- |
| 优先级   | P1      |
| 类型     | 功能    |
| 关联需求 | REQ-004 |

**测试步骤**：
| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 加载 SVG → 选中 3 个 | 选中项显示蓝色边框 |
| 2 | 颜色选择器选择红色 #ff0000 | 颜色预览更新 |
| 3 | 点击「改色」 | 选中的 3 个 SVG 变为红色 |
| 4 | 未选中的 SVG | 保持原色不变 |

---

### TC-009: 三种压缩模式

| 属性     | 值      |
| -------- | ------- |
| 优先级   | P1      |
| 类型     | 功能    |
| 关联需求 | REQ-006 |

**测试步骤**：
| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 选择图片 → 「有损压缩」→ 质量 60% → 压缩 | 压缩率较高（> 40%） |
| 2 | 重新选择 → 「无损压缩」→ 压缩 | 压缩率较低但不丢失质量 |
| 3 | 重新选择 → 「智能推荐」→ 压缩 | 根据格式自动选择参数 |
| 4 | 切换为「无损」时 | 质量滑块变为禁用状态 |

---

### TC-010: 多格式压缩验证

| 属性     | 值      |
| -------- | ------- |
| 优先级   | P1      |
| 类型     | 功能    |
| 关联需求 | REQ-005 |

**测试步骤**：
| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 选择 JPEG 文件 → 压缩 | 成功压缩，生成 `_compressed.jpg` |
| 2 | 选择 PNG 文件 → 压缩 | 成功压缩，生成 `_compressed.png` |
| 3 | 选择 WebP 文件 → 压缩 | 成功压缩，生成 `_compressed.webp` |
| 4 | 选择 GIF 文件 → 压缩 | 成功处理（可能压缩率低） |

---

### TC-011: 格式转换 — 多格式

| 属性     | 值      |
| -------- | ------- |
| 优先级   | P1      |
| 类型     | 功能    |
| 关联需求 | REQ-007 |

**测试步骤**：
| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | PNG → WebP | 生成 .webp 文件 |
| 2 | JPEG → PNG | 生成 .png 文件（含透明背景处理） |
| 3 | WebP → JPEG | 生成 .jpg 文件（白色背景填充） |
| 4 | PNG → AVIF | 生成 .avif 文件 |

---

### TC-012: 格式转换 — 预设尺寸

| 属性     | 值      |
| -------- | ------- |
| 优先级   | P1      |
| 类型     | 功能    |
| 关联需求 | REQ-008 |

**测试步骤**：
| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 选择图片 → 预设「Favicon 32×32」→ 转换 | 输出 32×32 图片 |
| 2 | 选择图片 → 预设「App Icon 512×512」→ 转换 | 输出 512×512 图片 |
| 3 | 选择图片 → 「保持原尺寸」→ 转换 | 输出尺寸与原图一致 |

---

## 拖拽与快捷键测试（P1）

### TC-013: 拖拽 SVG 文件

| 属性     | 值      |
| -------- | ------- |
| 优先级   | P1      |
| 类型     | 功能    |
| 关联需求 | REQ-009 |

**测试步骤**：
| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 在 SVG 查看页，从文件管理器拖入 .svg 文件 | 蓝色遮罩出现「📥 松开鼠标放入文件」 |
| 2 | 松开鼠标 | 遮罩消失，SVG 加载到宫格中 |
| 3 | 提示消息 | 显示「已通过拖拽添加 N 个 SVG 文件」 |

---

### TC-014: 拖拽图片到压缩页

| 属性     | 值      |
| -------- | ------- |
| 优先级   | P1      |
| 类型     | 功能    |
| 关联需求 | REQ-009 |

**测试步骤**：
| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 切换到「图片压缩」页 | - |
| 2 | 从文件管理器拖入 .png 文件 | 文件列表自动添加 |
| 3 | 拖入 .svg 文件 | 忽略，不添加（SVG 不在压缩格式列表中） |

---

### TC-015: 快捷键 Ctrl+O

| 属性     | 值      |
| -------- | ------- |
| 优先级   | P1      |
| 类型     | 功能    |
| 关联需求 | REQ-010 |

**测试步骤**：
| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 按 `Ctrl+O` | 弹出文件选择对话框 |
| 2 | 选择文件 → 确认 | 文件路径发送到渲染进程 |

---

### TC-016: 快捷键 Ctrl+Shift+O

| 属性     | 值      |
| -------- | ------- |
| 优先级   | P1      |
| 类型     | 功能    |
| 关联需求 | REQ-010 |

**测试步骤**：
| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 按 `Ctrl+Shift+O` | 弹出文件夹选择对话框 |
| 2 | 选择文件夹 → 确认 | 文件夹路径发送到渲染进程 |

---

## 边界测试（P2）

### TC-017: 空文件夹加载

| 属性     | 值      |
| -------- | ------- |
| 优先级   | P2      |
| 类型     | 边界    |
| 关联需求 | REQ-001 |

**测试步骤**：
| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 选择一个空文件夹 | 提示「已加载 0 个 SVG 文件」 |
| 2 | 页面状态 | 显示空状态，不崩溃 |

---

### TC-018: 超大图片压缩

| 属性     | 值      |
| -------- | ------- |
| 优先级   | P2      |
| 类型     | 边界    |
| 关联需求 | REQ-005 |

**测试步骤**：
| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 选择 > 10MB 的 JPEG | 文件正常添加到列表 |
| 2 | 点击压缩 | 压缩完成，不超时 |
| 3 | 验证输出 | 输出文件有效可打开 |

---

### TC-019: 批量文件处理

| 属性     | 值               |
| -------- | ---------------- |
| 优先级   | P2               |
| 类型     | 边界             |
| 关联需求 | REQ-005, REQ-007 |

**测试步骤**：
| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 选择 20+ 图片文件 | 全部添加到列表 |
| 2 | 点击压缩/转换 | 逐个处理，界面不卡死 |
| 3 | 验证结果 | 每个文件独立显示状态 |

---

## 异常测试（P2）

### TC-020: 损坏文件处理

| 属性     | 值      |
| -------- | ------- |
| 优先级   | P2      |
| 类型     | 异常    |
| 关联需求 | REQ-005 |

**测试步骤**：
| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 将一个 .txt 文件改名为 .png | - |
| 2 | 选择该文件 → 压缩 | 显示「失败」标签，不崩溃 |
| 3 | 批量中混入损坏文件 | 其他正常文件继续处理 |

---

### TC-021: 取消对话框

| 属性     | 值   |
| -------- | ---- |
| 优先级   | P2   |
| 类型     | 异常 |
| 关联需求 | 全局 |

**测试步骤**：
| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 点击「选择文件」→ 取消 | 无任何操作，不报错 |
| 2 | 点击「选择文件夹」→ 取消 | 无任何操作，不报错 |

---

### TC-022: 侧栏折叠

| 属性     | 值   |
| -------- | ---- |
| 优先级   | P2   |
| 类型     | 功能 |
| 关联需求 | 全局 |

**测试步骤**：
| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 点击侧栏折叠按钮 | 侧栏收缩为图标模式 |
| 2 | 内容区 | 自动扩展填满 |
| 3 | 再次点击 | 侧栏恢复展开 |

---

## UI 体验测试（P2）

### TC-023: 页面切换动画

| 属性     | 值   |
| -------- | ---- |
| 优先级   | P2   |
| 类型     | 功能 |
| 关联需求 | 全局 |

**测试步骤**：
| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 快速切换三个菜单项 | 页面平滑 slide 过渡，无闪烁 |
| 2 | 切换到有数据的页面再切走 | 切回时数据保持（Vue keep-alive） |

---

### TC-024: 暗色主题一致性

| 属性     | 值   |
| -------- | ---- |
| 优先级   | P2   |
| 类型     | 功能 |
| 关联需求 | 全局 |

**测试步骤**：
| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 检查侧栏 | 暗色背景 + 紫色渐变 Logo |
| 2 | 检查三个页面 | 无白色背景突兀区域 |
| 3 | 检查弹窗/提示 | Naive UI 暗色主题一致 |

---

## 测试统计

| 分类           | 数量   | 说明         |
| -------------- | ------ | ------------ |
| 冒烟测试（P0） | 4      | 必须全部通过 |
| 功能测试（P1） | 12     | 核心功能     |
| 边界测试（P2） | 3      | 极端情况     |
| 异常测试（P2） | 3      | 错误处理     |
| UI 测试（P2）  | 2      | 体验一致性   |
| **合计**       | **24** |              |

## 变更记录

| 日期       | 版本 | 变更内容 | 变更人 |
| ---------- | ---- | -------- | ------ |
| 2026-03-16 | V1.0 | 初始版本 | —      |
