# Image Toolkit 操作优化 - 任务清单

## 基本信息

| 项目         | 值                      |
| ------------ | ----------------------- |
| **所属迭代** | 2026-03-17 功能迭代     |
| **计划日期** | 2026-03-17 ~ 2026-03-21 |
| **总任务数** | 54 项                   |
| **创建日期** | 2026-03-17              |

---

## Phase 1：架构基础重构

> 📅 Day 1 上午 · 预计 5h · 里程碑 M1

### 样式与主题基础

- [ ] **T-001** 创建 CSS 变量文件 `variables.css`（浅色/深色双主题 token）`0.5h`
  - 📄 `src/styles/variables.css` [新建]
  - 📝 定义 `--text-primary`, `--text-secondary`, `--text-muted`, `--bg-page`, `--bg-card`, `--bg-card-hover`, `--bg-sider`, `--border-light`, `--accent`, `--success`, `--error` 等变量
  - 📝 浅色模式 `:root`，深色模式 `[data-theme="dark"]`
  - ✅ 两套主题在 devtools 切换 `data-theme` 属性后色值正确

- [ ] **T-002** 创建 `transitions.css`（TransitionGroup 入退场动画）`0.25h`
  - 📄 `src/styles/transitions.css` [新建]
  - 📝 定义 `list-enter-active`, `list-leave-active`, `list-move` 动画
  - ✅ 给测试列表加 TransitionGroup 后入场/退场有平滑动画

### Pinia 状态管理

- [ ] **T-003** 创建 `fileStore` — 文件列表管理 `1h`
  - 📄 `src/stores/file.store.ts` [新建]
  - 📝 实现 `FileEntry` 接口（path / name / size / type / status / progress / result / error）
  - 📝 actions: `addFiles`（追加+去重）、`removeFile`（单个删除）、`clearFiles`（清空）、`updateFileStatus`（状态更新）
  - 📝 getters: `fileCount`、`selectedCount`
  - ✅ 单元测试覆盖：追加去重、删除、清空、计数

- [ ] **T-004** 创建 `settingsStore` — 用户偏好持久化 `0.5h`
  - 📄 `src/stores/settings.store.ts` [新建]
  - 📝 state: `outputDir`（string|null）、`keepOriginalFile`（默认true）、`soundEnabled`（默认true）、`theme`
  - 📝 通过 IPC 调用 `config:get` / `config:set` 持久化
  - 🔗 依赖 T-010（electron-store 配置管理）
  - ✅ 修改偏好 → 重启应用 → 偏好保留

- [ ] **T-005** 创建 `undoStore` — 撤销/重做栈 `1h`
  - 📄 `src/stores/undo.store.ts` [新建]
  - 📝 `UndoableAction` 接口（type / timestamp / undo / redo / description）
  - 📝 undoStack + redoStack，最大容量 50
  - 📝 actions: `push`、`undo`、`redo`、`clear`
  - ✅ 单元测试覆盖：推入/撤销/重做/容量溢出丢弃最早记录

### 路由与 App.vue 重构

- [ ] **T-006** 路由独立到 `router/index.ts` `0.25h`
  - 📄 `src/router/index.ts` [新建]
  - 📄 `src/main.ts` [修改] — 移除内联路由
  - ✅ 三个路由（/svg, /compress, /convert）切换正常

- [ ] **T-007** 抽取 `useTheme` composable，App.vue 瘦身 `0.5h`
  - 📄 `src/composables/useTheme.ts` [新建]
  - 📄 `src/App.vue` [修改] — 移除主题逻辑，改用 `useTheme()`
  - 📝 `useTheme` 管理 `isDark`、`currentTheme`、`toggleTheme`，同步 `data-theme` 属性
  - 🔗 依赖 T-001
  - ✅ App.vue 行数从 ~248 减少 30%+

### 主进程新增

- [ ] **T-008** 创建 IPC 类型定义 `0.25h`
  - 📄 `electron/types/ipc.types.ts` [新建]
  - 📝 定义 `CompressOptions`, `CompressResult`, `ConvertOptions`, `ConvertResult`, `ExportPngOptions` 等

- [ ] **T-009** 新增 `system.handler.ts` `0.25h`
  - 📄 `electron/ipc/system.handler.ts` [新建]
  - 📄 `electron/main.ts` [修改] — 注册 handler
  - 📝 `system:openPath` — 使用 `shell.openPath()` 打开目录
  - ✅ 调用后打开系统文件管理器到指定目录

- [ ] **T-010** 新增 `config.ts` — electron-store 配置管理 `0.5h`
  - 📄 `electron/core/config.ts` [新建]
  - 📄 `electron/main.ts` [修改] — 注册 `config:get` / `config:set` IPC
  - 📝 安装 `electron-store` 依赖
  - ✅ 渲染进程可读写持久化配置

---

## Phase 2：通用组件抽取

> 📅 Day 1 下午 ~ Day 2 上午 · 预计 7.5h · 里程碑 M2

### 新建通用组件

- [ ] **T-011** 创建 `FileListItem.vue` — 单文件行 `1h`
  - 📄 `src/components/FileListItem.vue` [新建]
  - 📝 props: `file: FileEntry`, `showDelete`, `showProgress`, `selectable`
  - 📝 显示：文件名 / 大小 / 格式标签 / 状态 / 进度条 / ×删除按钮（hover 显示）
  - 📝 emits: `remove`, `select`, `preview`, `retry`
  - 📝 使用 CSS 变量着色，hover 高亮
  - 🔗 依赖 T-001
  - ✅ 渲染正确，hover 显示删除按钮，点击触发事件

- [ ] **T-012** 创建 `FileList.vue` — 虚拟列表容器 `1h`
  - 📄 `src/components/FileList.vue` [新建]
  - 📝 使用 Naive UI `<n-virtual-list>` 渲染上百文件
  - 📝 内部使用 `FileListItem` 作为行组件
  - 📝 支持 `TransitionGroup` 入退场动画（引用 T-002）
  - 🔗 依赖 T-011
  - ✅ 插入 200 条数据后滚动流畅，删除项有退场动画

- [ ] **T-013** 创建 `Toolbar.vue` — 工具栏 `0.5h`
  - 📄 `src/components/Toolbar.vue` [新建]
  - 📝 props: `fileCount`, `selectedCount?`, `isProcessing`
  - 📝 插槽: `#left`（左侧自定义按钮）、`#right`（右侧自定义按钮）
  - 📝 内置 "共 N 个文件" 标签 + "清空列表" 按钮
  - ✅ 文件计数跟随 store 实时更新

- [ ] **T-014** 创建 `DropZone.vue` — 拖拽区域 `0.5h`
  - 📄 `src/components/DropZone.vue` [新建]
  - 📝 空状态区域，点击选择文件 / 拖拽放入
  - 📝 显示接受的格式提示（根据 prop 动态显示）
  - ✅ 拖拽放入后触发 `onDrop` 事件

- [ ] **T-015** 创建 `OutputDirPicker.vue` — 输出目录选择器 `0.5h`
  - 📄 `src/components/OutputDirPicker.vue` [新建]
  - 📝 显示当前输出路径 + "选择" 按钮 + "重置为默认" 按钮
  - 📝 通过 `settingsStore` 读写路径记忆
  - 🔗 依赖 T-004, T-010
  - ✅ 选择目录后路径显示正确，重启后保留

### Composable 升级

- [ ] **T-016** 升级 `useFileDrop.ts` — 格式识别 + 自动路由 `0.5h`
  - 📄 `src/composables/useFileDrop.ts` [修改]
  - 📝 新增 `classifyFiles(paths)` — 按扩展名分类为 `{ svg, image }`
  - 📝 混合格式拖入时自动拆分到对应模块
  - 🔗 依赖 T-003
  - ✅ 拖入 SVG + PNG 混合文件，SVG 进入 SVG 模块，PNG 进入当前模块

### 三视图重构

- [ ] **T-017** 重构 `ImageCompress.vue` — 接入通用组件 `1h`
  - 📄 `src/views/ImageCompress.vue` [修改]
  - 📝 替换内联文件列表 → `<FileList>`
  - 📝 替换内联工具栏 → `<Toolbar>`
  - 📝 状态迁移到 `fileStore`，`selectFiles` 改为追加模式
  - 🔗 依赖 T-012, T-013, T-003
  - ✅ 功能与重构前一致，代码行数减少 40%+

- [ ] **T-018** 重构 `FormatConvert.vue` — 接入通用组件 `1h`
  - 📄 `src/views/FormatConvert.vue` [修改]
  - 📝 同 T-017 模式
  - 🔗 依赖 T-012, T-013, T-003
  - ✅ 功能一致，使用 `<FileList>` + `<Toolbar>`

- [ ] **T-019** 重构 `SvgViewer.vue` — 接入通用组件 `1h`
  - 📄 `src/views/SvgViewer.vue` [修改]
  - 📝 使用 `<FileList selectable>` 替代内联宫格
  - 📝 保留 SVG 特有的颜色修改功能
  - 🔗 依赖 T-012, T-013
  - ✅ SVG 宫格视图正常，选中交互正常

- [ ] **T-020** App.vue 接入 DropZone + useFileDrop `0.5h`
  - 📄 `src/App.vue` [修改]
  - 📝 移除内联 `handleDragOver/Leave/Drop`，改用 `useFileDrop` composable
  - 📝 拖拽遮罩替换为 `<DropZone>` 组件
  - 🔗 依赖 T-014, T-016
  - ✅ 拖拽行为与重构前一致

---

## Phase 3：第一批高优需求

> 📅 Day 2 下午 ~ Day 3 · 预计 7h · 里程碑 M3

- [ ] **T-021** 全局搜索替换硬编码色值 → CSS 变量 `1h` `F5.1`
  - 📄 全部 `.vue` 文件 [修改]
  - 📝 搜索 `rgba(255,255,255,`, `rgba(0,0,0,`, `#18181c`, `#667eea` 等 → `var(--xxx)`
  - 📝 验证浅色模式下无白底白字、深色模式下无黑底黑字
  - 🔗 依赖 T-001
  - ✅ 浅色/深色切换后全部组件颜色正确

- [ ] **T-022** 创建 `PreviewModal.vue` — SVG 大图预览 `2h` `F2.1.1`
  - 📄 `src/components/PreviewModal.vue` [新建]
  - 📝 props: `visible`, `svgContent`, `fileName`
  - 📝 功能：鼠标滚轮缩放 + 拖拽平移 + 缩放按钮（fit/100%/200%）
  - 📝 使用 Naive UI `<n-modal>` 作为弹窗容器
  - ✅ 点击缩略图弹出大图，缩放/平移流畅

- [ ] **T-023** `SvgViewer` 接入 PreviewModal `0.5h` `F2.1.1`
  - 📄 `src/views/SvgViewer.vue` [修改]
  - 📝 点击 SVG 缩略图时打开 `PreviewModal`
  - 🔗 依赖 T-022
  - ✅ 点击任意 SVG 可预览大图

- [ ] **T-024** 创建 `ExportPngDialog.vue` — PNG 导出设置 `1h` `F2.2.2`
  - 📄 `src/components/ExportPngDialog.vue` [新建]
  - 📝 两种模式切换：倍率（1x/2x/3x 复选）/ 自定义像素（宽×高 + 锁定比例）
  - 📝 输出目录选择（复用 `OutputDirPicker`）
  - ✅ 选择倍率/输入像素后点击导出功能正常

- [ ] **T-025** 升级 `svg:exportPng` IPC — 支持自定义像素 `0.5h` `F2.2.2`
  - 📄 `electron/ipc/svg.handler.ts` [修改]
  - 📝 新增 `customWidth` / `customHeight` 参数支持
  - 📝 使用 `sharp().resize(width, height)` 实现自定义尺寸
  - ✅ 导出的 PNG 尺寸与设置一致

- [ ] **T-026** `SvgViewer` 接入 ExportPngDialog `0.5h` `F2.2.2`
  - 📄 `src/views/SvgViewer.vue` [修改]
  - 📝 点击"导出 PNG"按钮弹出设置弹窗
  - 🔗 依赖 T-024, T-025
  - ✅ 完整导出流程可用

- [ ] **T-027** 压缩/转换完成后"打开输出文件夹"按钮 `0.5h` `F3.1.5` `F4.1.4`
  - 📄 `src/views/ImageCompress.vue`, `src/views/FormatConvert.vue` [修改]
  - 📝 完成后显示按钮，点击调用 `system:openPath`
  - 🔗 依赖 T-009
  - ✅ 点击后系统文件管理器打开到输出目录

- [ ] **T-028** 压缩模块接入 `OutputDirPicker` `0.5h` `F3.1.1`
  - 📄 `src/views/ImageCompress.vue` [修改]
  - 📝 工具栏增加 `<OutputDirPicker>`，选择的目录传入压缩参数
  - 🔗 依赖 T-015
  - ✅ 选择输出目录后压缩结果输出到指定目录

- [ ] **T-029** 格式转换增加"保留原文件"开关 `0.5h` `F4.1.1`
  - 📄 `src/views/FormatConvert.vue` [修改]
  - 📄 `electron/ipc/convert.handler.ts` [修改]
  - 📝 UI 增加 `<n-switch>` 绑定 `settingsStore.keepOriginalFile`
  - 📝 后端根据 `keepOriginal` 参数决定是否删除原文件
  - 🔗 依赖 T-004
  - ✅ 开关默认打开，关闭后转换会删除原文件

---

## Phase 4：第二批中优需求

> 📅 Day 3 ~ Day 4 · 预计 7.5h · 里程碑 M4

- [ ] **T-030** `fileStore.addFiles` 去重 + Toast 提示 `0.25h` `F1.1.4`
  - 📄 `src/stores/file.store.ts` [修改]
  - ✅ 重复文件拖入时跳过并 Toast "N 个文件已存在，已跳过"

- [ ] **T-031** `Toolbar` 显示"共 N 个文件"标签 `0.25h` `F1.1.5`
  - 📄 `src/components/Toolbar.vue` [修改]
  - ✅ 文件增减后标签实时更新

- [ ] **T-032** `useFileDrop` 混合格式自动拆分 `0.5h` `F1.2.1`
  - 📄 `src/composables/useFileDrop.ts` [修改]
  - ✅ 拖入 SVG+PNG 混合，SVG 进 `/svg`，PNG 保留当前模块

- [ ] **T-033** `PreviewModal` 增加源代码查看 + 复制 `1h` `F2.1.2` `F2.1.3`
  - 📄 `src/components/PreviewModal.vue` [修改]
  - 📝 标签页切换：预览 | 源代码（使用 `<n-tabs>`）
  - 📝 代码区 `<pre>` + "复制"按钮（使用 `navigator.clipboard.writeText`）
  - ✅ 切换到源代码标签可查看 SVG XML，点击复制成功

- [ ] **T-034** SVG 选中勾选角标 ✓ + 选中计数 `0.5h` `F2.2.1`
  - 📄 `src/views/SvgViewer.vue` [修改]
  - 📝 选中的 SVG 卡片右上角显示 ✓ 角标
  - 📝 工具栏显示"已选中 N 个"
  - ✅ 角标显示正确，计数跟随选择实时变化

- [ ] **T-035** 压缩中取消 — `compress:abort` `1h` `F3.1.4`
  - 📄 `electron/ipc/compress.handler.ts` [修改]
  - 📄 `src/views/ImageCompress.vue` [修改]
  - 📝 handler 增加 `AbortController` 信号检查
  - 📝 UI 增加"取消"按钮，点击调用 `compress:abort`
  - ✅ 压缩中点取消后停止处理，已完成项保留结果

- [ ] **T-036** 压缩失败单独重试 `0.5h` `F3.2.3`
  - 📄 `src/components/FileListItem.vue` [修改]
  - 📄 `src/views/ImageCompress.vue` [修改]
  - 📝 失败项显示"重试"按钮，点击后仅重新压缩该文件
  - ✅ 点击重试后单文件重新压缩

- [ ] **T-037** 每个文件增加压缩进度条 `0.5h` `F3.2.1`
  - 📄 `src/components/FileListItem.vue` [修改]
  - 📝 `status === 'processing'` 时显示 `<n-progress>` 动画
  - ✅ 压缩中每个文件有进度条动画

- [ ] **T-038** 压缩前预览缩略图 + 一键重新压缩 `0.5h` `F3.1.2` `F3.1.3`
  - 📄 `src/views/ImageCompress.vue` [修改]
  - 📝 文件列表项支持点击预览原图缩略图（使用 Naive UI `<n-image>`）
  - 📝 完成后工具栏显示"重新压缩"按钮
  - ✅ 可预览缩略图，调整参数后一键重新压缩

- [ ] **T-039** 格式转换自定义宽高（锁定比例） `1h` `F4.1.2`
  - 📄 `src/views/FormatConvert.vue` [修改]
  - 📄 `electron/ipc/convert.handler.ts` [修改]
  - 📝 UI 增加宽/高输入框 + 🔗 锁定比例按钮
  - 📝 后端 `sharp().resize(width, height, { fit: 'contain' })`
  - ✅ 输入宽高后转换结果尺寸正确，锁定比例时自动计算另一边

- [ ] **T-040** 格式兼容提示（源=目标时） `0.25h` `F4.1.3`
  - 📄 `src/views/FormatConvert.vue` [修改]
  - 📝 检测源文件格式与目标格式相同时弹 `<n-dialog>` 确认
  - ✅ PNG → PNG 时弹出提示

- [ ] **T-041** 格式转换"选择文件夹"批量加载 `0.5h` `F4.1.5`
  - 📄 `src/views/FormatConvert.vue` [修改]
  - 📝 工具栏增加"选择文件夹"按钮，递归读取目录内所有图片
  - ✅ 选择文件夹后批量加载所有支持格式的图片

- [ ] **T-042** 转换失败详情 hover 显示 `0.25h` `F4.2.1`
  - 📄 `src/components/FileListItem.vue` [修改]
  - 📝 失败项的 `<n-tag type="error">` 增加 `<n-tooltip>` 显示 `error` 信息
  - ✅ hover "失败"标签可看到具体错误原因

- [ ] **T-043** FileList + FileListItem 入退场动画 `0.5h` `F5.2`
  - 📄 `src/components/FileList.vue` [修改]
  - 📝 使用 `<TransitionGroup>` 包裹列表项
  - 🔗 依赖 T-002
  - ✅ 添加/删除文件时有平滑动画

- [ ] **T-044** FileListItem hover 背景高亮 + 阴影 `0.25h` `F5.3`
  - 📄 `src/components/FileListItem.vue` [修改]
  - 📝 CSS: `hover → background: var(--bg-card-hover); box-shadow: 0 2px 8px rgba(0,0,0,0.1)`
  - 🔗 依赖 T-001
  - ✅ hover 时有明显视觉反馈

- [ ] **T-045** 创建 `useKeyboard.ts` — 快捷键系统 `1h` `F1.3.1~3`
  - 📄 `src/composables/useKeyboard.ts` [新建]
  - 📝 注册 `Ctrl+Z`（undo）、`Ctrl+Shift+Z`（redo）、`Ctrl+A`（selectAll）、`Delete`（removeSelected）
  - 📝 在 App.vue 中调用 `useKeyboard()`
  - 🔗 依赖 T-005
  - ✅ 快捷键触发对应操作

---

## Phase 5：第三批低优需求

> 📅 Day 4 ~ Day 5 · 预计 4h · 里程碑 M5

- [ ] **T-046** `DropZone` 按模块显示接受格式 `0.25h` `F1.2.2`
  - 📄 `src/components/DropZone.vue` [修改]
  - ✅ SVG 模块显示"支持 SVG"，压缩模块显示"支持 JPEG/PNG/WebP…"

- [ ] **T-047** `DropZone` 空状态拖拽动画指引 `0.25h` `F1.2.3`
  - 📄 `src/components/DropZone.vue` [修改]
  - 📝 增加上下浮动动画 + 虚线边框呼吸动画
  - ✅ 空状态下有明确的拖拽引导

- [ ] **T-048** `PreviewModal` SVG 背景色切换 `0.5h` `F2.1.4`
  - 📄 `src/components/PreviewModal.vue` [修改]
  - 📝 底部工具条增加三个按钮：棋盘格 / 白色 / 深色
  - ✅ 切换后预览区背景正确变化

- [ ] **T-049** SVG 批量打包 ZIP 下载 `1h` `F2.2.3`
  - 📄 `electron/ipc/svg.handler.ts` [修改]
  - 📄 `src/views/SvgViewer.vue` [修改]
  - 📝 安装 `archiver` 依赖
  - 📝 新增 `svg:downloadZip` IPC，选中 SVG 打包 ZIP
  - ✅ 选中多个 SVG 后下载 ZIP 文件

- [ ] **T-050** 压缩结果前后大小对比柱状图 `0.5h` `F3.2.2`
  - 📄 `src/views/ImageCompress.vue` [修改]
  - 📝 每个文件结果区增加小型柱状图（原始/压缩 两根柱子）
  - 📝 使用 CSS 实现简易柱状图，无需第三方图表库
  - ✅ 完成后每个文件可视化对比大小

- [ ] **T-051** 文件格式专属图标/颜色标签 `0.25h` `F4.2.2`
  - 📄 `src/components/FileListItem.vue` [修改]
  - 📝 根据 `file.type` 映射不同颜色的 `<n-tag>`（PNG→蓝, JPG→橙, SVG→紫…）
  - ✅ 不同格式文件显示不同颜色标签

- [ ] **T-052** 大量文件加载骨架屏 `0.5h` `F5.5`
  - 📄 `src/components/FileList.vue` [修改]
  - 📝 加载中状态显示 `<n-skeleton>` 占位符
  - ✅ 加载 200 个文件时有骨架屏过渡

- [ ] **T-053** 创建 `useAudio.ts` — 操作音效 `0.5h` `F5.4`
  - 📄 `src/composables/useAudio.ts` [新建]
  - 📄 `src/assets/sounds/success.mp3`, `error.mp3` [新建]
  - 📝 使用 Web Audio API 播放
  - 📝 通过 `settingsStore.soundEnabled` 控制开关
  - 📝 音效文件通过工具生成（Web Audio oscillator 合成短提示音）
  - ✅ 操作完成/失败有提示音，设置中可关闭

- [ ] **T-054** `Toolbar` 按钮 Tooltip 显示快捷键 `0.25h` `F1.3.4`
  - 📄 `src/components/Toolbar.vue` [修改]
  - 📝 "清空列表" → Tooltip "清空列表"、"开始压缩" → Tooltip "开始压缩 (Ctrl+Enter)" 等
  - ✅ 按钮 hover 时 Tooltip 包含快捷键提示

---

## 交付检查清单

### Phase 1 完成标准

- [ ] 所有 CSS 变量在浅色/深色模式下正确
- [ ] fileStore + undoStore 单元测试全部通过
- [ ] App.vue 行数减少 30%+
- [ ] electron-store 持久化读写正常

### Phase 2 完成标准

- [ ] 三视图代码行数各减少 40%+
- [ ] 文件列表使用虚拟列表，200 文件渲染 < 100ms
- [ ] 拖拽自动路由正常工作

### Phase 3 完成标准

- [ ] 浅色模式全局无色值异常
- [ ] SVG 大图预览 + 导出 PNG 完整可用
- [ ] 输出目录选择+路径记忆+打开目录 完整可用

### Phase 4 完成标准

- [ ] 快捷键 Ctrl+Z/A/Delete 正常工作
- [ ] 压缩取消/重试/进度条 正常工作
- [ ] 文件列表 hover + 动画 效果良好

### Phase 5 完成标准

- [ ] 低优需求全部实现
- [ ] 全量回归测试通过

---

## 关联文档

| 文档        | 路径                                                                     |
| ----------- | ------------------------------------------------------------------------ |
| 开发计划    | [功能迭代-开发计划.md](../plan/功能迭代-开发计划.md)                     |
| V2 架构设计 | [工具库-架构设计-V2.md](../architecture/工具库-架构设计-V2.md)           |
| 需求规格    | [功能迭代-需求规格.md](../demand/2026.3.17功能迭代/功能迭代-需求规格.md) |

## 变更记录

| 日期       | 版本 | 变更内容     | 变更人 |
| ---------- | ---- | ------------ | ------ |
| 2026-03-17 | V1.0 | 初始任务清单 | —      |
